# Photoshop Lab: GrayBit-7

React-приложение для первой лабораторной по цифровому представлению изображения.

## Возможности

- загрузка PNG, JPG/JPEG и GrayBit-7 (`.gb7`);
- отображение изображения на HTML5 Canvas;
- скачивание изображения в PNG, JPG и GB7;
- ручная реализация кодера и декодера GrayBit-7;
- строка состояния с шириной, высотой и глубиной цвета;
- переключение каналов RGB/Gray/Alpha для просмотра;
- пипетка с координатами, RGB и LAB выбранного пикселя;
- масштабирование, подгонка под область просмотра и закрытие файла.

## Запуск

```bash
npm install
npm run dev
```

## Сборка

```bash
npm run build
```

## Хостинг

Опубликованная страница: <ВСТАВЬТЕ_ССЫЛКУ_NETLIFY>

Деплой выполняется автоматически из ветки `main` на Netlify
(конфигурация в [`netlify.toml`](netlify.toml)).
